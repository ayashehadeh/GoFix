import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primaryDark   = Color(0xFF012249); // Navy blue (Figma: #012249)
  static const Color primaryOrange = Color(0xFFF3800D); // GoFix orange (Figma: #F3800D)

  // Backgrounds
  static const Color background    = Color(0xFFFCFCFC); // Page bg (Figma: #FCFCFC)
  static const Color surface       = Color(0xFFF8F9FA); // Card / category bg (Figma: #F8F9FA)
  static const Color white         = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary   = Color(0xFF012249);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color textHint      = Color(0xFFBDBDBD);

  // Misc
  static const Color divider       = Color(0xFFE0E0E0);
  static const Color error         = Color(0xFFE53935);
  static const Color star          = Color(0xFFF3800D);
}
